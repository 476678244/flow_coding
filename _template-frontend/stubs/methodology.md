# 方法论 — `<FEATURE_TITLE>`

SoT：本文件。实现与 E2E 须对齐；冲突时改代码或改本合同并记入 acceptance，禁止静默漂移。

## 1. 目标

`<进行中用户能看见并做什么。完成后必须怎样。>`

## 2. 可见即可续

```text
看得见的 X  →  跨过 <边界>  →  仍能做 Y
```

## 3. 作用域

| 在范围内 | 不在范围内 |
|----------|------------|
| `<进行中组件>` | `<冻结的完成后组件>` |

## 4. 控件合同

| 控件 | `data-testid` | 何时可见 | 行为 |
|------|----------------|----------|------|
| | | | |

## 5. 数据合同

| 来源（SSE / store / 磁盘） | 可见面必须 |
|---------------------------|------------|
| | |

无数据时：`<空态诚实文案>`。禁止假数据填空。

## 6. 生命周期

1. 出现  
2. 实时更新  
3. 卸掉 / persist  
4. 跨边界后仍能做 Y  

## 7. Fail Fast

| 禁止 | 要求 |
|------|------|
| | |
