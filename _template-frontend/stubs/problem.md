# 问题 — `<FEATURE_TITLE>`

## 驱动场景

用户做 `<动作>`。看见 `<现网可见物>`。下一步想 `<Y>`，但做不到 / 看不清，因为 `<缺口>`。

## 现网事实（Phase 0 headed）

证据：`~/Downloads/safe_claw_worksapce/workspace/<probe>-during.png` · `<probe>-after.png`

| 态 | 人看见什么 | DOM / testid / class |
|----|------------|----------------------|
| 进行中 | | |
| 完成后 | | |

## 根因

- 数据断点：`<字段>` 到了 `<层>` 但可见面没用 / 被丢掉  
- 改错文件风险：真路径 `<features/...>` ；死代码 `<src/components/...>`  

## 可见即可续

```text
看得见的 X  →  跨过 <边界>  →  仍能做 Y
```

现网：`<已成立 / 不成立，差在哪>`。  
禁止用 `<错误续法，例如把临时卡写进历史>` 来续。

## 成功时可见差异

| 之前（进行中） | 之后（进行中） |
|----------------|----------------|
| | |

| 之前（完成后） | 之后（完成后） |
|----------------|----------------|
| | 相同 / 或明确列出允许的变化 |

## 非本问题

-  
