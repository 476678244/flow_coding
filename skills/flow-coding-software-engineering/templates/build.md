# build · __feature_id__

正式跟踪用 [track/04-build.md](track/04-build.md)。once 与 keep 分栏，禁止混成「测过了」。

Phase 0 现网是 **once**。TDD 入库文件是 **keep**。

performance tuning：默认不做。仅当 classify 写了性能，或出现全库两两比较 / 无超时抓取时才收。
