# deliver · __feature_id__

用户没要求不要 git commit / 开 PR。不要提交 `data/`、`.venv/`、`__pycache__/`、`logs/**/runtime/`。

| 项 | 是否改 | 路径与 why（没有新 why 不要新开长文） |
| --- | --- | --- |
| docs | 是 / 否 | `docs/product.md` · `architecture.md` · `decisions.md` · `usage.md` · `glossary.md` |
| guide | 是 / 否 | 人怎么打开：后端 `http://127.0.0.1:8000` · 前端 `http://127.0.0.1:5173` |
| product design thinking | 必写 | 读者看见什么；今天是否更少更准；故意不做的是什么 → `notes.md` |

不要：多用户、OAuth、云同步、未读红点、无限信息流、去掉落选列表。
