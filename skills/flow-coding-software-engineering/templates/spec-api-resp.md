## 5. API 响应

无 HTTP 面则整节写「无 API，合同在模块函数」。禁止编造未实现的字段。空数据与失败必须诚实。

合同：看得见的 X → 跨过 &lt;边界&gt; → 仍能做 Y

### 请求

- 方法 / 路径：
- 查询或体：

### 成功（摘录）

```json
{}
```

### 空数据

```json
{}
```

### 失败

```json
{}
```
